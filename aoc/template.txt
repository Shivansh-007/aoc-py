import aoc
from aoc.utils import AoCInput


def parse_data(raw):
    data = AoCInput.get_lines(raw)
    return data


def part_one(raw):
    data = parse_data(raw)
    ...


def part_two(raw):
    data = parse_data(raw)
    ...


if __name__ == "__main__":
    aoc.watch()